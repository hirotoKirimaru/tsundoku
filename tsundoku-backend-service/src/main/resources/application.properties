spring:
  datasource:
    url: ${DATABASE_URL_SPRINGBOOT:jdbc:postgresql://localhost:15432/tsundoku}
    username: ${DATABASE_USER:tsundokuuser}
    password: ${DATABASE_PASS:tsundokupass}
    driver-class-name: org.postgresql.Driver

server:
  servlet:
    context-path: /api
